<svg xmlns="http://www.w3.org/2000/svg" width="240" height="240" viewBox="0 0 240 240">

    <defs>
        <linearGradient id="bgGrad" x1="0" y1="0" x2="0" y2="1">
            <stop offset="0%" stop-color="#4f8df7"/>
            <stop offset="100%" stop-color="#1e4fbf"/>
        </linearGradient>
    </defs>

    <!-- Background -->
    <rect x="0" y="0" width="240" height="240" rx="28" fill="url(#bgGrad)" />

    <!-- HK2 -->
    <text x="120" y="95" text-anchor="middle" font-size="56" font-weight="700" font-family="Arial, Helvetica, sans-serif" fill="#ffffff">
    HK2
    </text>

    <!-- Divider -->
    <rect x="60" y="115" width="120" height="2" fill="#ffffff" opacity="0.4"/>

    <!-- Title -->
    <text x="120" y="150" text-anchor="middle" font-size="26" font-weight="600" font-family="Arial, Helvetica, sans-serif" fill="#ffffff">
    Add Bootstrap 5
    </text>

    <!-- Subtitle -->
    <text x="120" y="178" text-anchor="middle" font-size="14" font-family="Arial, Helvetica, sans-serif" fill="#e6ecff">
    for Magento 2
    </text>

</svg>
